# تقرير التحقق — 31 يوليو 2026

## فحوص اجتازتها الحزمة

- التحقق من وجود مشروع MAUI ومشروع Core ومشروع الاختبارات.
- عدم وجود ملفات Dart أو `pubspec.yaml` داخل النسخة المحولة.
- التحقق من أن المبالغ تستخدم `long` بوحدة نقدية صغرى.
- التحقق من إنشاء اسم قاعدة مختلف لكل Firebase UID.
- التحقق من أن قواعد Firestore تعتمد `request.auth.uid`.
- التحقق من وجود مزامنة سجلًا بسجل وحل تعارضات وحذف tombstone.
- التحقق من مستورد نسخ Flutter JSON القديمة.
- التحقق من XML لجميع ملفات XAML وملفات المشاريع وManifest.
- التحقق من JSON وYAML.
- التحقق من ربط أسماء أحداث XAML بدوال code-behind.
- التحقق من توازن الأقواس في ملفات C# مع تجاهل النصوص والتعليقات.
- التحقق من عدم تضمين ملفات خطوط في الحزمة.
- التحقق من إزالة API التنبيه القديم `DisplayAlert` الخاص بما قبل .NET 10.

## ما لم يمكن تشغيله في بيئة التحويل

بيئة العمل التي تم فيها التحويل لا تحتوي .NET SDK أو MAUI workload أو Android SDK، لذلك لم يتم تنفيذ:

- `dotnet restore`
- `dotnet test`
- بناء APK أو AAB
- بناء نسخة Windows

تمت إضافة GitHub Actions وسكربتات بناء لتنفيذ هذه الفحوص على بيئة .NET MAUI فعلية. يجب اعتبار أول تشغيل ناجح للـ CI هو التحقق التنفيذي النهائي قبل النشر.
