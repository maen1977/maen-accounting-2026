# حالة التحويل

## مكتمل في هذه الحزمة

- إنشاء حل .NET 10 MAUI منظم.
- فصل Core عن الواجهة.
- Android وWindows.
- واجهات عربية RTL: الدخول، الرئيسية، الإدخال، التقارير، الإعدادات.
- قاعدة SQLite لكل مستخدم.
- دقة مالية بأعداد صحيحة.
- Firebase Auth عبر REST.
- Firestore Sync سجلًا بسجل.
- نسخ واستيراد من Flutter.
- قواعد Firestore جديدة.
- اختبارات Core وCI.

## يلزم تنفيذه على جهاز تطوير MAUI

- تشغيل `dotnet restore` و`dotnet test` وBuild لأن بيئة التحويل الحالية لا تحتوي .NET SDK أو Android SDK.
- نشر قواعد Firestore الجديدة.
- تجربة تسجيل الدخول على مشروع Firebase الفعلي.
- تجهيز مفتاح توقيع Android وإعداد المتجر.
- اختبار واجهة Windows وAndroid يدويًا على أحجام مختلفة.

## قرار التصميم

لم يتم نقل الطقس وتحديد الموقع وأسعار السوق؛ لأنها غير محاسبية، وكانت ميزة السوق غير موصولة فعليًا في Flutter، كما أن تحديد الموقع كان يتم من عنوان IP دون موافقة واضحة. يمكن إضافتها لاحقًا كوحدات اختيارية منفصلة إذا ثبتت الحاجة.
