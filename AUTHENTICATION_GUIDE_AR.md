# دليل الدخول واسترجاع كلمة المرور

## الدخول المحلي بدون تسجيل

عند تشغيل التطبيق يمكن اختيار **الدخول محليًا بدون تسجيل**. لا يحتاج هذا الخيار إلى بريد إلكتروني أو كلمة مرور، وينشئ التطبيق هوية ثابتة للجهاز ويحفظ البيانات في قاعدة محلية منفصلة.

هذا الوضع مناسب لمن يريد بدء العمل مباشرة أو استخدام التطبيق دون اتصال. البيانات لا تُرفع إلى Firebase تلقائياً، ولا يمكن فتح قاعدة البيانات المحلية على جهاز آخر إلا بعد تصدير نسخة احتياطية أو ربط الاستخدام لاحقاً بحساب سحابي.

## الدخول السحابي

للمزامنة والنسخ الاحتياطي السحابي، اختر **الدخول السحابي** ثم أدخل البريد الإلكتروني وكلمة المرور. يحتاج الحساب إلى كلمة مرور من ستة أحرف على الأقل. يمكن إنشاء حساب جديد من الزر الموجود أسفل نموذج الدخول.

## استرجاع كلمة المرور

إذا نسي المستخدم كلمة المرور، يكتب بريده في خانة البريد ثم يضغط **نسيت كلمة المرور؟**. يرسل Firebase رسالة إعادة تعيين إلى البريد عند وجود حساب مطابق. يجب فتح الرابط الموجود في الرسالة واختيار كلمة مرور جديدة، ثم العودة إلى التطبيق وتسجيل الدخول بالكلمة الجديدة.

يعرض التطبيق رسالة عامة لا تكشف ما إذا كان البريد مسجلاً أم لا، وهذا يحمي خصوصية الحسابات. ينبغي فحص مجلد Spam أو الرسائل غير المرغوبة إذا لم تصل الرسالة خلال دقائق. يجب أن يكون المستخدم قادراً على الوصول إلى البريد نفسه؛ فلا يمكن للتطبيق استعادة كلمة مرور أو تجاوز ملكية البريد.

## ملاحظات تقنية

يُرسل طلب الاستعادة إلى Firebase باستخدام نوع الطلب `PASSWORD_RESET`. لا يحفظ التطبيق كلمات المرور ولا ينشئ كلمات مرور مؤقتة. يحتفظ التطبيق بالمسار المحلي القديم المتعلق بالبريد للتوافق مع النسخ المحلية السابقة، بينما يستخدم الدخول المحلي الجديد هوية الجهاز تلقائياً.
