# خارطة تحويل معن للمحاسبة إلى نظام محاسبي متكامل

## الهدف

تحويل التطبيق الحالي من سجل يومي للمبيعات والتكاليف والمصروفات إلى نظام محاسبة عملي يعتمد **القيد المزدوج**، مع تشغيل محلي أولاً ومزامنة سحابية اختيارية، ودعم التوسع إلى الفواتير والعملاء والموردين والمخزون والتقارير المالية.

## النطاق الوظيفي

| المرحلة | الوحدة | النتيجة المتوقعة |
|---|---|---|
| 1 | النواة المحاسبية | دليل حسابات، قيود يومية، تحقق من توازن المدين والدائن، حالات مسودة ومرحل وملغى |
| 2 | الحسابات الافتتاحية | أرصدة افتتاحية قابلة للتعديل وترحيل آمن للسجلات الحالية |
| 3 | الأطراف والفواتير | العملاء، الموردون، فواتير البيع والشراء، سندات القبض والصرف |
| 4 | المخزون | الأصناف، وحدات القياس، حركات الإدخال والإخراج، تكلفة المبيعات، الجرد |
| 5 | التقارير | دفتر الأستاذ، ميزان المراجعة، قائمة الدخل، الميزانية العمومية، التدفق النقدي، أعمار الذمم |
| 6 | الإنتاج | النسخ الاحتياطي، المزامنة، الصلاحيات، سجل التدقيق، التصدير والطباعة، تحسينات الأداء |

## قواعد محاسبية غير قابلة للكسر

1. كل قيد مرحل يجب أن يحتوي سطراً مديناً واحداً على الأقل وسطراً دائناً واحداً على الأقل.
2. مجموع المدين في القيد يساوي مجموع الدائن بوحدة العملة الصغرى، ولا يسمح النظام بترحيل قيد غير متوازن.
3. القيد المرحل لا يُحذف؛ يتم إلغاؤه بقيد عكسي يحافظ على أثر التدقيق.
4. الحسابات الأساسية هي: الأصول، الالتزامات، حقوق الملكية، الإيرادات، والمصروفات.
5. كل سجل محاسبي يحمل معرّف المستخدم ووقت الإنشاء والتعديل والجهاز، حتى تبقى عزل البيانات والمزامنة ممكنين.
6. لا يتم افتراض نظام ضريبي أو نسبة ضريبة محددة قبل تحديد بلد التشغيل؛ تُبنى الضرائب كإعدادات قابلة للتهيئة.

## التوافق مع البيانات الحالية

سيستمر دعم جدول `profit_entries` ونسخ JSON القديمة. عند الترحيل، يُنشأ قيد انتقال متوازن لكل سجل موجود باستخدام حسابات افتراضية قابلة للتعديل:

- المبيعات: مدين لحساب الصندوق أو الذمم المدينة، ودائن لحساب إيرادات المبيعات.
- التكلفة: مدين لحساب تكلفة المبيعات، ودائن لحساب المخزون أو حساب تسوية انتقالية عند عدم توفر بيانات المخزون.
- المصروفات: مدين لحساب المصروفات التشغيلية، ودائن لحساب الصندوق أو الدائنين.

لن يتم حذف السجلات القديمة أثناء الترحيل، وستكون العملية قابلة لإعادة التشغيل دون إنشاء قيود مكررة.

## قرار التنفيذ الأول

ستبدأ الجولة التالية ببناء طبقة `Core` مستقلة تشمل نماذج الحساب والقيد والسطر المحاسبي ومحرك التحقق والتقارير الأساسية، ثم تُربط بقاعدة SQLite والواجهة. هذا يقلل خطر إدخال منطق محاسبي داخل صفحات MAUI ويجعل الاختبارات قابلة للتشغيل دون جهاز Android.

## إعدادات تحتاج تحديداً لاحقاً

لإكمال ملاءمة الإصدار التجاري، يلزم تحديد بلد الاستخدام، العملة الرئيسية، هل المطلوب ضريبة قيمة مضافة، هل الفاتورة تشمل بيعاً نقدياً وآجلاً، وهل المخزون مطلوب من اليوم الأول. في حال عدم تحديدها، سيُبنى النظام بإعدادات عامة قابلة للتغيير دون افتراضات ضريبية.

## خارطة طريق قصيرة

سيكون أول إصدار عملي هو **دفتر محاسبي متوازن**: دليل حسابات، قيد يومية، ترحيل وإلغاء، أرصدة الحسابات، وميزان مراجعة. بعد التحقق منه تضاف الفواتير والعملاء والموردون، ثم المخزون والتقارير المتقدمة.
